<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="gitgaya.com.rssfeednews">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/logo_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:roundIcon="@mipmap/logo_round"
        tools:ignore="GoogleAppIndexingWarning">

       <activity android:name=".view.SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.MainActivity" />
        <activity
            android:name=".viewmodel.RSSFeedActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".viewmodel.BrowserActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".model.Textreadwrite" />
    </application>

</manifest>